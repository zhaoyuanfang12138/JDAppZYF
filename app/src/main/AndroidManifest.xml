<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.jdappzyf">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:name=".utils.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.view.LoginActivity" />
        <activity android:name=".regist.view.RegistActivity" />
        <activity android:name=".sort.view.DetailActivity" />
        <activity android:name=".first.view.HomeActivity" />
        <activity android:name=".login.view.CloseActivity" />
        <activity android:name=".first.view.ItemActivity" />
        <activity android:name=".first.view.SearchPageActivity" />
        <activity android:name=".first.view.WebViewActivity" />
        <activity android:name=".login.view.AddressActivity" />
        <activity android:name=".login.view.AddAddressActivity"></activity>
    </application>

</manifest>